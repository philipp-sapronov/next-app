const { buttons, hero, pipeline, pricing, why, teachers, feedbacks, leading, common } = require("./ru")

module.exports = {
  ru: {
    buttons,
    common,
    feedbacks,
    hero,
    leading,
    pipeline,
    pricing,
    teachers,
    why,
  },
}

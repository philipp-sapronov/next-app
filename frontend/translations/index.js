const { buttons, hero, pipeline, pricing, why, teachers, feedbacks, leading } = require("./ru")

module.exports = {
  ru: {
    buttons,
    hero,
    pipeline,
    pricing,
    why,
    teachers,
    feedbacks,
    leading,
  },
}

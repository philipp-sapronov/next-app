version: "3.4"

networks:
  main:
    driver: bridge

services:
  proxy:
    image: umputun/nginx-le:latest
    container_name: proxy
    restart: unless-stopped
    volumes:
      - ./proxy/ssl:/etc/nginx/ssl
      - ./proxy/conf/service-proxy.conf:/etc/nginx/service.conf
      # - ./etc/service-example-2.conf:/etc/nginx/service2.conf # more services, should be service*.conf
    ports:
      - "80:80"
      - "443:443"
    environment:
      - TZ=Europe/Kiev
      - LETSENCRYPT=true
      - LE_EMAIL=ph.soff@gmail.com
      - LE_FQDN=www.iep.com.ua,iep.com.ua
    depends_on:
      - frontend
      - api
    networks:
      - main

  api:
    build:
      context: ./api
    container_name: api
    restart: unless-stopped
    env_file:
      - ./.env
    command: "yarn start"
    networks:
      - main

  frontend:
    build:
      context: frontend
      target: prod
      args:
        - NODE_ENV=${NODE_ENV}
    container_name: frontend
    restart: unless-stopped
    volumes:
      - ./frontend/nginx.conf:/etc/nginx/nginx.conf
    networks:
      - main

volumes:
  letsencrypt:
